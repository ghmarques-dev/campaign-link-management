services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: campaign_link_management
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
